<h1>プロフィール画面</h1>

<%= @stylist.name %>
<%= @stylist.gender %>
<%= @stylist.age %>
<%= rating_for @stylist, 'clothes', disable_after_rate: true, imdb_avg: true %>
<h4>'#{@stylist.name}'さんの評価</h4>
<% @stylist.reviews.each do |r| %>
    <%= r.title %>
    <%= r.comment %>
  <% end %>
<% if @stylist.id == current_user.id?? %>
<%= link_to 'プロフィールを編集する', edit_stylist_path(current_user.id) %>
<%= link_to 'アドバイスを受けたいユーザー一覧をみる', seekers_path %>
<% end %>
<% if @seeker.id == current_user.id? %>
<%= link_to 'メッセージ', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post %>
